import Nivo from "@/components/nivo";
import PageTemplate from "@/components/pageTemplate";

const NivoPage = () => {
    return (
        <PageTemplate title="Nivo">
            <Nivo />
        </PageTemplate>
    );
};

export default NivoPage;
